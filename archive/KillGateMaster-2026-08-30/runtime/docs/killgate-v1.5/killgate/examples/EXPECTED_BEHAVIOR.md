# Expected Behavior

## Test 13 — Commodity supporting, strong core
Input: fraud model plus email alerts at $499/mo.
Expected: email = supporting / REMOVE_DEFER. Do not RESEARCH_PIVOT solely because email is bundled. Score the model.

## Test 14 — Vendor domains ≠ demand independence
Input: three FSM help centers document routing.
Expected: feature-existence and source diversity. Not three independent demand observations.

## Test 15 — Bundled downstream vs pivot
Input: ChangeOrder Guard as locked. Detection from comms is novel. Incumbents already e-sign change orders. SMS labeled supporting or unlabeled.
Expected: M4 REMOVE_DEFER. M3 REMOVE_DEFER if owning a second portal is not why they pay $129. RESEARCH_PASS with implementation lock “write draft CO into existing PM; no parallel e-sign product” if the WTP sentence (detect missed extras before free work, $129) still holds. RESEARCH_PIVOT only if the locked hypothesis made a proprietary approval stack material to WTP.

## Test 16 — Discover labels without hints
Input: mixed offer with one differentiated mechanism, one useful commodity, one implementation detail, one shaky core assumption. User does not mark CORE/SUPPORTING.
Expected: infer and classify before scoring. WTP should attach to the differentiated mechanism if evidence supports that attachment. Do not require the user to pre-label.

## Test 17 — Bundled differentiator still pivots
Input: $99 SMS-confirmation product when incumbents already sell SMS confirmation as a named feature.
Expected: SMS is a bundled differentiator. RESEARCH_PIVOT or KILL that offer. Do not KEEP it as downstream infrastructure.

## Test 18 — Load-bearing capability before WTP
Input: $249 tool that predicts hidden collision damage before teardown from the same photos and estimate the insurer already used. Missed labor ops already have $25-$150 tools.
Expected: Split omitted-ops vs hidden-damage vs delay-reduction vs $249 WTP. Do not RESEARCH_PASS into eight $249 conversations. FEASIBILITY_REQUIRED. Lock a pre-teardown-input vs post-teardown-truth test and thresholds first. Do not KILL because hidden damage is not visible today. Do not treat cheap missed-ops tools as proof the family is dead.
